---
import { env } from "@/lib/env";
import ApplyParams from "./ApplyParams.astro";
import type { BaseProps } from "./types";

interface Props extends BaseProps {}

const { page } = Astro.props;

// タイトルの決定ロジック
let target = page.title;
if (env.targetRegion === "US" && page.params.titleUs) {
  target = page.params.titleUs;
} else if (env.targetRegion === "CN" && page.params.titleCn) {
  target = page.params.titleCn;
}
---

<ApplyParams target={target} params={env} />
